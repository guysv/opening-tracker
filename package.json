{
  "name": "personal-opening-explorer",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bun run serve.dev.ts",
    "dev:hot": "bun run --hot serve.dev.ts",
    "build": "bun run build.ts",
    "preview": "bun run build.ts && bunx serve dist",
    "check": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/bun": "^1.3.11",
    "typescript": "^5.9.3"
  },
  "dependencies": {
    "bitboard-chess": "^0.4.0",
    "pgn-parser": "^2.2.1",
    "preact": "^10.29.0",
    "preact-iso": "^2.11.1"
  }
}
